import {
  articleInfo
} from 'dummy/helpers/article-info';
import {
  module,
  test
} from 'qunit';

module('Unit | Helper | article info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = articleInfo([]);
  assert.equal(result, '');

  // TODO: full tests
});
